# llms.txt

Oratos can generate a conservative `llms.txt` draft from discovered pages:

```bash
oratos generate llms ./priv/static --output llms.txt
```

`llms.txt` is an emerging convention. Treat it as a supplement to:

- good HTML semantics and metadata
- sitemaps and internal linking
- structured data (JSON-LD)

