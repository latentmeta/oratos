# v0.2 backlog (from #26)

Create these GitHub issues when splitting [Architectural assessment #26](https://github.com/latentmeta/oratos/issues/26).

## Issue: Per-rule registry refactor

**Labels:** `architecture`, `v0.2`

Replace category rule structs with one struct per `rule_id` and a central registry. Prerequisite for severity overrides.

## Issue: SiteRule vs PageRule trait split

**Labels:** `architecture`, `v0.2`

Move site-level checks (`llm.missing-llms-txt`, future robots/sitemap site checks) out of `audit.rs` ad hoc logic into `SiteRule` implementations.

## Issue: Lightweight HtmlPage / optional PageSource

**Labels:** `architecture`, `v0.2`

Stop storing full HTML `source` on every `HtmlPage` by default; load on demand for prompt generation.

## Issue: Sync CLI with async URL loader

**Labels:** `architecture`, `v0.2`

Use sync CLI entry and isolate `tokio` to `oratos-html` URL/crawl paths only.

## Issue: Tracing spans for audit pipeline

**Labels:** `observability`, `v0.2`

Emit `tracing` spans for load, extract, audit, render — or remove unused subscriber deps.
